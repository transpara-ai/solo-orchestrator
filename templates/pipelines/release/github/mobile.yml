# =============================================================================
# Mobile Release Pipeline — __PROJECT_NAME__
# =============================================================================
#
# This template contains four self-contained sections, one per framework path.
# Your language selection determines which section to use:
#
#   typescript / javascript  →  Section 1: Expo / EAS Build (React Native)
#   dart                     →  Section 2: Flutter
#   kotlin / java            →  Section 3: Native Android (Gradle)
#   swift                    →  Section 4: Native iOS (Xcode)
#
# Instructions:
#   1. Uncomment the section that matches your framework.
#   2. Delete the other sections entirely.
#   3. Each section has its own setup steps — no shared placeholders are needed
#      because the toolchains differ significantly between frameworks.
#
# =============================================================================

name: Release — __PROJECT_NAME__
# ─────────────────────────────────────────────────────────────────
# BEFORE FIRST RELEASE: Search for "TODO" in this file and configure:
#   - Code signing (iOS certificates, Android keystore)
#   - App store credentials (App Store Connect, Google Play)
#   - Build configuration for your framework section
# The Phase 3→4 gate will warn if TODOs remain unconfigured.
# Light-track projects: this pipeline is optional for internal-only use.
# ─────────────────────────────────────────────────────────────────

on:
  push:
    tags: ['v*']
  workflow_dispatch:

permissions:
  contents: write

# ============================================================
# SECTION 1 — EXPO / EAS BUILD (React Native)
# Uncomment this section if using Expo. Delete other sections.
# ============================================================

# jobs:
#   build-and-submit:
#     runs-on: ubuntu-latest
#
#     steps:
#       - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7 (v7.0.1)
#
#       - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7 (v7.0.0)
#         with:
#           node-version: '20'
#
#       - name: Install dependencies
#         run: npm ci
#
#       - name: Setup Expo
#         uses: expo/expo-github-action@eab7a230208c952974db8c3245cfd78402c7b385 # v9 (9.0.0)
#         with:
#           eas-version: latest
#           token: ${{ secrets.EXPO_TOKEN }}
#
#       - name: Build
#         run: eas build --profile production --platform all --non-interactive
#
#       - name: Submit to app stores
#         run: eas submit --platform all --non-interactive
#         # TODO: Configure eas.json submit profiles before enabling this step.
#         #   ios:     Set appleId, ascAppId in eas.json → submit → production → ios
#         #   android: Set serviceAccountKeyPath in eas.json → submit → production → android
#         #            Upload google-service-account.json as a repo secret or EAS secret.
#
#       - name: Create GitHub Release
#         uses: softprops/action-gh-release@3d0d9888cb7fd7b750713d6e236d1fcb99157228 # v3 (v3.0.2)
#         with:
#           generate_release_notes: true

# ============================================================
# SECTION 2 — FLUTTER
# Uncomment this section if using Flutter. Delete other sections.
# ============================================================

# jobs:
#   build-android:
#     runs-on: ubuntu-latest
#
#     steps:
#       - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7 (v7.0.1)
#
#       - uses: actions/setup-java@03ad4de0992f5dab5e18fcb136590ce7c4a0ac95 # v5 (v5.6.0)
#         with:
#           distribution: temurin
#           java-version: '17'
#
#       - uses: subosito/flutter-action@1a449444c387b1966244ae4d4f8c696479add0b2 # v2 (v2.23.0)
#         with:
#           channel: stable
#
#       - name: Install dependencies
#         run: flutter pub get
#
#       - name: Decode Android keystore
#         run: echo "${{ secrets.ANDROID_KEYSTORE_BASE64 }}" | base64 -d > android/app/release.keystore
#
#       - name: Build Android App Bundle
#         run: flutter build appbundle --release
#         env:
#           ANDROID_KEYSTORE_PASSWORD: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
#           ANDROID_KEY_ALIAS: ${{ secrets.ANDROID_KEY_ALIAS }}
#           ANDROID_KEY_PASSWORD: ${{ secrets.ANDROID_KEY_PASSWORD }}
#         # NOTE: Your android/app/build.gradle must be configured to read signing
#         # config from these env vars (or generate a key.properties file in a
#         # prior step). Flutter does not consume keystore env vars automatically.
#
#       - name: Upload Android artifact
#         uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7 (v7.0.1)
#         with:
#           name: __PROJECT_NAME__-android
#           path: build/app/outputs/bundle/release/*.aab
#
#   build-ios:
#     runs-on: macos-latest
#
#     steps:
#       - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7 (v7.0.1)
#
#       - uses: subosito/flutter-action@1a449444c387b1966244ae4d4f8c696479add0b2 # v2 (v2.23.0)
#         with:
#           channel: stable
#
#       - name: Install dependencies
#         run: flutter pub get
#
#       - name: Build iOS IPA
#         run: flutter build ipa --release --export-options-plist=ios/ExportOptions.plist
#         # TODO: Configure iOS code signing before enabling this step:
#         #   1. Create ExportOptions.plist with your team ID and provisioning profile.
#         #   2. Import your Apple Distribution Certificate into the macOS runner keychain.
#         #      Store APPLE_CERTIFICATE_BASE64 and APPLE_CERTIFICATE_PASSWORD as secrets.
#         #   3. Install the provisioning profile on the runner.
#
#       - name: Upload iOS artifact
#         uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7 (v7.0.1)
#         with:
#           name: __PROJECT_NAME__-ios
#           path: build/ios/ipa/*.ipa
#
#   release:
#     needs: [build-android, build-ios]
#     runs-on: ubuntu-latest
#
#     steps:
#       - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8 (v8.0.1)
#
#       - name: Create GitHub Release
#         uses: softprops/action-gh-release@3d0d9888cb7fd7b750713d6e236d1fcb99157228 # v3 (v3.0.2)
#         with:
#           files: |
#             __PROJECT_NAME__-android/*
#             __PROJECT_NAME__-ios/*
#           generate_release_notes: true
#
#       # TODO: Submit to app stores via Fastlane:
#       #   iOS:     fastlane deliver (or upload_to_testflight for beta)
#       #   Android: fastlane supply (upload_to_play_store)
#       #   See the Mobile Platform Module for Fastlane configuration examples.

# ============================================================
# SECTION 3 — NATIVE ANDROID (Gradle)
# Uncomment this section if using native Android. Delete other sections.
# ============================================================

# jobs:
#   build-android:
#     runs-on: ubuntu-latest
#
#     steps:
#       - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7 (v7.0.1)
#
#       - uses: actions/setup-java@03ad4de0992f5dab5e18fcb136590ce7c4a0ac95 # v5 (v5.6.0)
#         with:
#           distribution: temurin
#           java-version: '17'
#
#       - name: Decode Android keystore
#         run: echo "${{ secrets.ANDROID_KEYSTORE_BASE64 }}" | base64 -d > app/release.keystore
#
#       - name: Build release bundle
#         run: ./gradlew bundleRelease
#         env:
#           ANDROID_KEYSTORE_PASSWORD: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
#           ANDROID_KEY_ALIAS: ${{ secrets.ANDROID_KEY_ALIAS }}
#           ANDROID_KEY_PASSWORD: ${{ secrets.ANDROID_KEY_PASSWORD }}
#
#       - name: Upload artifact
#         uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7 (v7.0.1)
#         with:
#           name: __PROJECT_NAME__-android
#           path: app/build/outputs/bundle/release/*.aab
#
#       - name: Create GitHub Release
#         uses: softprops/action-gh-release@3d0d9888cb7fd7b750713d6e236d1fcb99157228 # v3 (v3.0.2)
#         with:
#           files: app/build/outputs/bundle/release/*.aab
#           generate_release_notes: true
#
#       # TODO: Submit to Play Store via Fastlane:
#       #   fastlane supply --aab app/build/outputs/bundle/release/app-release.aab --track internal
#       #   Requires GOOGLE_PLAY_JSON_KEY secret (service account key).

# ============================================================
# SECTION 4 — NATIVE iOS (Xcode / Swift)
# Uncomment this section if using native iOS (Swift). Delete other sections.
# Requires macOS runner. See the Mobile Platform Module for signing details.
# ============================================================

# jobs:
#   build-ios:
#     runs-on: macos-latest
#
#     steps:
#       - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7 (v7.0.1)
#
#       - name: Select Xcode version
#         run: sudo xcode-select -s /Applications/Xcode_16.0.app/Contents/Developer
#         # TODO: Update the Xcode version to match your project's requirements.
#         #   List available versions on the runner: ls /Applications/Xcode*.app
#
#       - name: Install signing certificate
#         env:
#           APPLE_CERTIFICATE_BASE64: ${{ secrets.APPLE_CERTIFICATE_BASE64 }}
#           APPLE_CERTIFICATE_PASSWORD: ${{ secrets.APPLE_CERTIFICATE_PASSWORD }}
#           KEYCHAIN_PASSWORD: ${{ secrets.KEYCHAIN_PASSWORD }}
#         run: |
#           # Create a temporary keychain for signing
#           CERTIFICATE_PATH=$RUNNER_TEMP/build_certificate.p12
#           KEYCHAIN_PATH=$RUNNER_TEMP/app-signing.keychain-db
#
#           echo -n "$APPLE_CERTIFICATE_BASE64" | base64 --decode -o $CERTIFICATE_PATH
#           security create-keychain -p "$KEYCHAIN_PASSWORD" $KEYCHAIN_PATH
#           security set-keychain-settings -lut 21600 $KEYCHAIN_PATH
#           security unlock-keychain -p "$KEYCHAIN_PASSWORD" $KEYCHAIN_PATH
#           security import $CERTIFICATE_PATH -P "$APPLE_CERTIFICATE_PASSWORD" -A -t cert -f pkcs12 -k $KEYCHAIN_PATH
#           security set-key-partition-list -S apple-tool:,apple: -k "$KEYCHAIN_PASSWORD" $KEYCHAIN_PATH
#           security list-keychain -d user -s $KEYCHAIN_PATH
#         # TODO: Configure signing secrets before enabling:
#         #   APPLE_CERTIFICATE_BASE64   — base64-encoded .p12 distribution certificate
#         #   APPLE_CERTIFICATE_PASSWORD — the password for the .p12 file
#         #   KEYCHAIN_PASSWORD          — any temporary password for the CI keychain
#
#       - name: Install provisioning profile
#         env:
#           PROVISIONING_PROFILE_BASE64: ${{ secrets.PROVISIONING_PROFILE_BASE64 }}
#         run: |
#           PP_PATH=$RUNNER_TEMP/build_pp.mobileprovision
#           echo -n "$PROVISIONING_PROFILE_BASE64" | base64 --decode -o $PP_PATH
#           mkdir -p ~/Library/MobileDevice/Provisioning\ Profiles
#           cp $PP_PATH ~/Library/MobileDevice/Provisioning\ Profiles/
#         # TODO: Export your provisioning profile from Apple Developer Portal,
#         #   base64-encode it, and store as PROVISIONING_PROFILE_BASE64 secret.
#
#       - name: Resolve Swift packages
#         run: xcodebuild -resolvePackageDependencies -workspace __PROJECT_NAME__.xcworkspace -scheme __PROJECT_NAME__
#         # TODO: If using a .xcodeproj (no workspace), replace -workspace with -project
#         #   and update the file extension to .xcodeproj.
#
#       - name: Build and archive
#         run: |
#           xcodebuild -workspace __PROJECT_NAME__.xcworkspace \
#             -scheme __PROJECT_NAME__ \
#             -configuration Release \
#             -archivePath $RUNNER_TEMP/__PROJECT_NAME__.xcarchive \
#             -sdk iphoneos \
#             archive \
#             CODE_SIGN_STYLE=Manual
#         # TODO: Verify -scheme matches your Xcode project scheme name.
#
#       - name: Export IPA
#         run: |
#           xcodebuild -exportArchive \
#             -archivePath $RUNNER_TEMP/__PROJECT_NAME__.xcarchive \
#             -exportPath $RUNNER_TEMP/output \
#             -exportOptionsPlist ios/ExportOptions.plist
#         # TODO: Create ios/ExportOptions.plist with your team ID, signing method,
#         #   and provisioning profile name. Example:
#         #   <?xml version="1.0" encoding="UTF-8"?>
#         #   <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "...">
#         #   <plist version="1.0">
#         #   <dict>
#         #     <key>method</key><string>app-store-connect</string>
#         #     <key>teamID</key><string>YOUR_TEAM_ID</string>
#         #     <key>signingStyle</key><string>manual</string>
#         #     <key>provisioningProfiles</key>
#         #     <dict>
#         #       <key>com.yourcompany.yourapp</key>
#         #       <string>Your Profile Name</string>
#         #     </dict>
#         #   </dict>
#         #   </plist>
#
#       - name: Upload IPA artifact
#         uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7 (v7.0.1)
#         with:
#           name: __PROJECT_NAME__-ios
#           path: ${{ runner.temp }}/output/*.ipa
#
#       - name: Create GitHub Release
#         uses: softprops/action-gh-release@3d0d9888cb7fd7b750713d6e236d1fcb99157228 # v3 (v3.0.2)
#         with:
#           files: ${{ runner.temp }}/output/*.ipa
#           generate_release_notes: true
#
#       - name: Clean up keychain
#         if: always()
#         run: security delete-keychain $RUNNER_TEMP/app-signing.keychain-db
#
#       # TODO: Submit to App Store via Fastlane or xcrun:
#       #   Option A (Fastlane): fastlane deliver --ipa $RUNNER_TEMP/output/*.ipa
#       #   Option B (xcrun):    xcrun altool --upload-app -f $RUNNER_TEMP/output/*.ipa -t ios \
#       #                          --apiKey ${{ secrets.ASC_API_KEY_ID }} \
#       #                          --apiIssuer ${{ secrets.ASC_ISSUER_ID }}
#       #   Both require App Store Connect API key secrets.

  # ──────────────────────────────────────────────────────────
  # SBOM Generation (uncomment in your active build section)
  # ──────────────────────────────────────────────────────────
  # Add this step to your chosen build job before release creation:
  #
  #     - name: Generate SBOM
  #       run: |
  #         # Node.js (React Native): npx @cyclonedx/cyclonedx-npm --output-file sbom.json
  #         # Dart (Flutter):         dart pub global activate cyclonedx --source git --git-url https://github.com/ArcticGizmo/cyclonedx-dart.git && cyclonedx -o sbom.json
  #         # Kotlin (Native):        ./gradlew cyclonedxBom
  #         # Swift (Native):         swift package show-dependencies --format json > sbom.json
  #         echo "Configure language-specific SBOM generation"
  #       continue-on-error: true
  #
  # ──────────────────────────────────────────────────────────
  # DAST — Dynamic Application Security Testing
  # ──────────────────────────────────────────────────────────
  # Mobile apps expose APIs that should be tested dynamically.
  # Add OWASP ZAP scanning against your backend API:
  #
  #     - name: DAST — OWASP ZAP baseline scan
  #       run: |
  #         docker run --rm ghcr.io/zaproxy/zaproxy:stable zap-baseline.py \
  #           -t ${{ vars.API_BASE_URL }} \
  #           -J zap-report.json
  #
  # For apps with no backend API, DAST is not applicable.
